<manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools"
package="submission2">
<uses-permission android:name="android.permission.INTERNET" />
<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.MyApplication"
    tools:ignore="AllowBackup">

    <activity
        android:name="com.example.submission2.SplashActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity
    android:name="com.example.submission2.MainActivity"
    android:exported="true"/>
    <activity
        android:name="com.example.submission2.ProfileActivity"
        android:exported="true" />
    <activity
        android:name="com.example.submission2.favorite.FavoriteUsersActivity"
        android:exported="true"/>
    <activity
        android:name="com.example.submission2.SettingActivity"
        android:exported="true"/>
</application>

</manifest>




